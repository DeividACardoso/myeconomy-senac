import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
        fontSize: 16,
        justifyContent: "flex-start",
        textAlign: "center",
        paddingBottom: 40,
        color: 'black'
    },
});