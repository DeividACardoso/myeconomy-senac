import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: "bold",
        justifyContent: "flex-start",
        textAlign: "center",
        paddingBottom: 8,
        color: 'black'
    },
});